<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.project.mapper.UserMapper">

    <resultMap id="UserEntityMap" type="org.example.project.model.UserEntity">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="displayName" column="display_name"/>
        <result property="email" column="email"/>
        <result property="password" column="password"/>
        <result property="role" column="role"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <insert id="insert" parameterType="org.example.project.model.UserEntity" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (username, display_name, email, password, role, status, created_at)
        VALUES (#{username}, #{displayName}, #{email}, #{password}, #{role}, #{status}, #{createdAt})
    </insert>

    <select id="findById" resultMap="UserEntityMap">
        SELECT id, username, display_name, email, password, role, status, created_at
        FROM users
        WHERE id = #{id}
    </select>

    <select id="findByUsername" resultMap="UserEntityMap">
        SELECT id, username, display_name, email, password, role, status, created_at
        FROM users
        WHERE username = #{username}
    </select>

    <select id="findByEmail" resultMap="UserEntityMap">
        SELECT id, username, display_name, email, password, role, status, created_at
        FROM users
        WHERE email = #{email}
    </select>

    <select id="list" resultMap="UserEntityMap">
        SELECT id, username, display_name, email, password, role, status, created_at
        FROM users
        <where>
            <if test="keyword != null and keyword != ''">
                (
                username LIKE CONCAT('%', #{keyword}, '%')
                OR display_name LIKE CONCAT('%', #{keyword}, '%')
                OR email LIKE CONCAT('%', #{keyword}, '%')
                )
            </if>
            <if test="role != null and role != ''">
                AND role = #{role}
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
        ORDER BY id ASC
    </select>

    <select id="count" resultType="long">
        SELECT COUNT(1)
        FROM users
        <where>
            <if test="keyword != null and keyword != ''">
                (
                username LIKE CONCAT('%', #{keyword}, '%')
                OR display_name LIKE CONCAT('%', #{keyword}, '%')
                OR email LIKE CONCAT('%', #{keyword}, '%')
                )
            </if>
            <if test="role != null and role != ''">
                AND role = #{role}
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
    </select>

    <update id="updateRole">
        UPDATE users
        SET role = #{role}
        WHERE id = #{userId}
    </update>

    <update id="updateStatus">
        UPDATE users
        SET status = #{status}
        WHERE id = #{userId}
    </update>

    <update id="updateDisplayName">
        UPDATE users
        SET display_name = #{displayName}
        WHERE id = #{userId}
    </update>

    <select id="countAll" resultType="long">
        SELECT COUNT(1) FROM users
    </select>

    <select id="countRoleDistribution" resultType="map">
        SELECT role AS label, COUNT(1) AS value
        FROM users
        GROUP BY role
        ORDER BY value DESC, role ASC
    </select>
</mapper>
