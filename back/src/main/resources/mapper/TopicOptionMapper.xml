<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.project.mapper.TopicOptionMapper">

    <resultMap id="TopicOptionEntityMap" type="org.example.project.model.TopicOptionEntity">
        <id property="id" column="id"/>
        <result property="topicId" column="topic_id"/>
        <result property="text" column="text"/>
        <result property="voteCount" column="vote_count"/>
    </resultMap>

    <insert id="insertBatch">
        INSERT INTO topic_option (topic_id, text, vote_count)
        VALUES
        <foreach collection="options" item="text" separator=",">
            (#{topicId}, #{text}, 0)
        </foreach>
    </insert>

    <select id="findById" resultMap="TopicOptionEntityMap">
        SELECT id, topic_id, text, vote_count
        FROM topic_option
        WHERE id = #{id}
    </select>

    <select id="listByTopicId" resultMap="TopicOptionEntityMap">
        SELECT id, topic_id, text, vote_count
        FROM topic_option
        WHERE topic_id = #{topicId}
        ORDER BY id ASC
    </select>

    <select id="listByTopicIds" resultMap="TopicOptionEntityMap">
        SELECT id, topic_id, text, vote_count
        FROM topic_option
        WHERE topic_id IN
        <foreach collection="topicIds" item="topicId" open="(" separator="," close=")">
            #{topicId}
        </foreach>
        ORDER BY topic_id ASC, id ASC
    </select>

    <update id="increaseVoteCount">
        UPDATE topic_option
        SET vote_count = CASE
            WHEN vote_count + #{delta} &lt; 0 THEN 0
            ELSE vote_count + #{delta}
        END
        WHERE id = #{optionId}
    </update>

</mapper>

